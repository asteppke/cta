##

epicsEnvSet SYS MTEST-PC-BY84

require cta

devSeqCtrlInit

dbLoadRecords("test_ctrlSeries.db", "SYS=$(SYS), P=$(SYS)-CTA:")

dbLoadRecords("seqCtrl.db", "P=$(SYS)-CTA:seq0Ctrl-, FLOAD=$(SYS)-CTA:seq0Ser0-Load, FINDEX=$(SYS)-CTA:Drv, TRA=1")

dbLoadRecords("series.db", "P=$(SYS)-CTA:seq0Ser0, LENGTH=6")

